import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scada.transformator',
  appName: 'Transformator Monitoring',
  webDir: 'dist'
};

export default config;
